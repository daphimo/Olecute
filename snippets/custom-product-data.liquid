{% assign selected = product.selected_or_first_available_variant %}
{% capture variants %}[
{% for variant in product.variants %}
  {% capture price %}{% render 'custom-product-price', product: product, variant: variant, show_discount: true %}{% endcapture %}
  {% assign image = variant.featured_image | default: product.featured_image %}
  {"id":{{ variant.id | append: '' | json }},"available":{{ variant.available | json }},"options":{{ variant.options | json }},"price":{{ variant.price | money | json }},"priceHtml":{{ price | json }},"image":{% if image %}{{ image | image_url: width: 720 | json }}{% else %}""{% endif %},"chest":{{ variant.metafields.custom.chest.value | json }},"waist":{{ variant.metafields.custom.waist.value | json }},"hips":{{ variant.metafields.custom.hips.value | json }},"bust":{{ variant.metafields.custom.bust.value | json }},"to_fit_waist":{{ variant.metafields.custom.to_fit_waist.value | json }},"pyjama_waist":{{ variant.metafields.custom.pyjama_waist.value | json }},"inseam_length":{{ variant.metafields.custom.inseam_length.value | json }},"front_length":{{ variant.metafields.custom.front_length.value | json }},"across_shoulder":{{ variant.metafields.custom.across_shoulder.value | json }}}{% unless forloop.last %},{% endunless %}
{% endfor %}]{% endcapture %}
data-variants="{{ variants | escape }}" data-selected="{{ selected.id }}" data-option-names="
{{- product.options | json | escape -}}
"
